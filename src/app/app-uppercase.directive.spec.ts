import { AppUppercaseDirective } from './app-uppercase.directive';

describe('AppUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new AppUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
